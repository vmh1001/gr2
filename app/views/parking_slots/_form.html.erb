<%= form_with(model: parking_slot) do |form| %>
<% if parking_slot.errors.any? %>
<div id="error_explanation" class="bg-red-50 p-6 rounded text-red-800">
  <h2 class="text-lg font-semibold mb-3"><%= pluralize(parking_slot.errors.count, "error") %> prohibited this parking_slot from being saved:</h2>

  <ul class="list-disc leading-relaxed">
    <% parking_slot.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

  <div class="mb-6">
        <%= form.label :parking_slot_type, class: label_class %>
          <%= form.text_field :parking_slot_type, class: input_class %>
      </div>
  <div class="mb-6">
        <%= form.label :parking_slot_description, class: label_class %>
          <%= form.text_field :parking_slot_description, class: input_class %>
      </div>

<%= form.submit class: button_class(theme: 'primary') %>

<% if parking_slot.persisted? %>
<%= link_to "Cancel", parking_slot, class: "text-neutral-700 underline inline-block ml-3" %>
<% else %>
<%= link_to "Cancel", parking_slots_path, class:  "text-neutral-700 underline inline-block ml-3" %>
<% end %>
<% end %>
